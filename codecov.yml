codecov:
  require_ci_to_pass: false
  disable_default_path_fixes: true

# Coverage is produced inside the CI workspace, so strip that prefix to get repository-relative paths.
fixes:
  - "/__w/tesseract_trac_ik/tesseract_trac_ik/target_ws/src/::"

coverage:
  precision: 2
  round: down
  range: "70...100"
  status:
    project:
      default:
        threshold: 0.5%

parsers:
  gcov:
    branch_detection:
      conditional: yes
      loop: yes
      method: no
      macro: no

comment:
  layout: "reach,diff,flags,tree"
  behavior: default
  require_changes: true
